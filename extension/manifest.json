{
    "name": "Open In Browser",
    "description": "View documents directly in the browser instead of downloading them. Allows you to change the MIME-type as well.",
    "version": "1.0",
    "manifest_version": 2,
    "background": {
        "scripts": ["background.js"],
        "persistent": true
    },
    "options_page": "options.html",
    "permissions": [
        "tabs",
        "webRequest",
        "webRequestBlocking",
        "<all_urls>"
    ]
}
